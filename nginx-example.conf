# Sample nginx configuration for /enigma path
# Add this to your nginx sites configuration

server {
    listen 80;
    server_name developersandbox.xyz;

    # Handle /enigma requests
    location /enigma/ {
        proxy_pass http://127.0.0.1:4000/enigma/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Handle other locations...
}</content>
<parameter name="filePath">/mnt/internalssd/Projects/javascript-enigma-machine/nginx-example.conf